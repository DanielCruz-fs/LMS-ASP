@model Library.Models.Catalog.AssetIndexModel;

<div id="assets">
    <h3>Library Catalog</h3>
    <div id="assetsTable">
        <table>
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Title</th>
                    <th>Author / Director</th>
                    <th>Dewey Call Number</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var asset in Model.Assets)
                {
                    <tr class="assetRow">
                        <td class="">
                            <a asp-controller="Catalog" asp-action="Detail" asp-route-id="@asset.Id">
                                <img src="https://mppl.org/wp-content/themes/mppl_responsive/library/images/no_image_available.png"
                                     class="imageCell" style="width: 60px; height: 60px; margin: 2px;" />
                            </a>
                        </td>
                        <td class="">@asset.Title</td>
                        <td class="">@asset.AuthorOrDirector</td>
                        <td class="">@asset.DeweyCallNumber</td>
                    </tr>

                }
            </tbody>
        </table>
    </div>
</div>
